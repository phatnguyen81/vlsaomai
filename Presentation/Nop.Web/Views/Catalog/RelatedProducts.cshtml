@model IList<ProductOverviewModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    <h3 style="text-transform:none; border-top:3px #ddd solid; line-height:40px;">@T("Products.RelatedProducts")</h3>
    <div class="row productList">
         @foreach (var item in Model)
{
        @Html.Partial("_ProductBoxSmall", item)
}
    </div>
    @*<div class="related-products-grid product-grid">
        <div class="title">
            <strong>@T("Products.RelatedProducts")</strong>
        </div>
        @foreach (var item in Model)
        {
            <div class="item-box">@Html.Partial("_ProductBox", item)</div>
        }
    </div>*@
}