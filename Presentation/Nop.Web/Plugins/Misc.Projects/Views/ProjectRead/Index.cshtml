@model ProjectIndexModel
    @{
        Layout = "~/Views/Shared/_ColumnsTwoIntro.cshtml";
    }
    @section left{
        @Html.Action("InfoBlock", "Common")
    }
    @section YouAreHere{
        <div class="youAreHere">
            <ul>
                <li><a href="@Url.RouteUrl("Homepage")">@T("Homepage")</a></li>
                <li class="active"><a href="#">@T("Toi.Plugin.Misc.Projects.Project")</a></li>
            </ul>
        </div>
    }
    <h4 class="titleIntroDiv">@T("Toi.Plugin.Misc.Projects.Project")</h4>
    <div class="row rightContent">
        @foreach (var project in Model.Projects)
        {
            <div class="row col-xs-12 col_right projectItems">
                <h5>@Html.Raw(project.Title)</h5>
                <div class="col-sm-7">
                    @Html.Raw(project.Full)
                </div>
                <div class="col-sm-5 imageProject" style="float:right;">
                    <div class="crop">
                        <img src="@project.PictureModel.ImageUrl">
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="row breakPages">
    @Html.Pager(Model.PagingFilteringContext).QueryParam("pagenumber")
</div>
